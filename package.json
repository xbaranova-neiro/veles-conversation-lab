{
  "name": "veles-conversation-lab",
  "private": true,
  "type": "module",
  "scripts": { "start": "node --env-file-if-exists=.env server.mjs", "dev": "node --env-file-if-exists=.env server.mjs", "test": "node --test tests/*.test.mjs" },
  "engines": { "node": ">=22" }
}
